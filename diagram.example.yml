root:
    .box.dashed&label=homelab:
        .box: nested
        .: content
        .icon.abs.bottom.red: lock
    .folder: |
        multiline
        string
    .box.skew.red: colored skewed box!

links:
